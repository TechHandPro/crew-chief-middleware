{
  "mcpServers": {
    "{{module}}": {
      "command": "python",
      "args": ["/absolute/path/to/{{name}}/server.py"],
      "env": {
        "{{env_prefix}}_BASE_URL": "{{base_url_json}}"
      }
    }
  }
}

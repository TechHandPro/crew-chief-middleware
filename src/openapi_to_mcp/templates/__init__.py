"""Templates for generated MCP server projects.

This is a package (not a bare directory) so ``importlib.resources`` can read the
``.tmpl`` files from an installed wheel as well as from a source checkout.
"""
